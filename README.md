# help-center-assets

Public relay + provenance store for Carform Help Center article images.
Intercom fetches these raw URLs and re-hosts each image on its own CDN; the copy here is the versioned source of record. Managed by `help-center/tools/publish/`.
